# praise-songs

찬양 아카이브 정적 웹앱. 순수 HTML/CSS/JS + Supabase(Edge Functions, Postgres) 백엔드로 구성된 프로젝트입니다.

## 프로젝트 구조

- `index.html`, `admin.html` — 사용자 페이지 / 관리자 페이지
- `app.js`, `style.css` — 메인 앱 로직과 스타일
- `js/api.js`, `js/config.js` — API 호출 래퍼, 백엔드(Supabase) 연결 설정
- `praise.json`, `public/praise.json` — 찬양 데이터 (같은 파일이 두 곳에 존재하므로 수정 시 동기화 필요)
- `songs.csv` — 원본 CSV 데이터
- `supabase/schema.sql`, `supabase/functions/{api,praise}` — DB 스키마와 Edge Function(백엔드 API)
- `scripts/*.mjs`, `scripts/*.sql`, `scripts/*.py` — 데이터 가져오기/내보내기, 마이그레이션용 1회성 스크립트

빌드 도구나 패키지 매니저 없이 정적 파일을 그대로 배포합니다(GitHub Pages, `CNAME` 참고).

## 작업 시 유의사항

- `praise.json`을 수정하면 `public/praise.json`도 함께 갱신할 것.
- 백엔드 전환/캐시 버전 관련 커밋 이력이 있으므로(`config.js`), 백엔드 URL이나 캐시 태그를 바꿀 때는 관련 파일(`config.js`, `manifest.json`, service worker 캐시 이름 등)을 전부 함께 갱신할 것.
- 관리자 인증은 `ADMIN_SECRET` 환경변수로 통일되어 있음(Supabase Edge Function 쪽 `authCheck` 액션 참고).

## 코딩 가이드라인 (Karpathy Guidelines)

LLM이 코딩할 때 흔히 저지르는 실수를 줄이기 위한 행동 지침입니다. 속도보다 신중함에 무게를 두므로, 사소한 작업에는 상황에 맞게 유연하게 적용하세요.

### 1. 코딩 전에 먼저 생각하기

**추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프는 드러낼 것.**

구현하기 전에:
- 가정을 명시적으로 밝힌다. 확신이 없으면 물어본다.
- 해석이 여러 가지로 갈릴 수 있으면 조용히 하나를 고르지 말고 선택지를 제시한다.
- 더 단순한 방법이 있다면 그렇게 말한다. 필요하면 반박(push back)한다.
- 불명확한 부분이 있으면 멈춘다. 무엇이 헷갈리는지 짚어서 질문한다.

### 2. 단순함이 최우선

**문제를 해결하는 최소한의 코드만 작성한다. 추측성 코드는 넣지 않는다.**

- 요청받지 않은 기능을 추가하지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청하지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 대한 에러 처리를 하지 않는다.
- 200줄로 쓴 코드가 50줄로 될 수 있다면 다시 쓴다.

스스로에게 물어볼 것: "시니어 엔지니어가 보면 과하게 복잡하다고 할까?" 그렇다면 단순화한다.

### 3. 외과수술 같은 변경 (Surgical Changes)

**반드시 건드려야 할 부분만 건드린다. 자신이 만든 부산물만 정리한다.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷팅을 임의로 "개선"하지 않는다.
- 고장 나지 않은 것을 리팩터링하지 않는다.
- 자신의 취향과 다르더라도 기존 스타일을 따른다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않는다.

변경으로 인해 고아(orphan)가 생기면:
- 자신의 변경 때문에 더 이상 쓰이지 않게 된 import/변수/함수는 제거한다.
- 요청받지 않았다면 원래 있던 죽은 코드는 제거하지 않는다.

체크리스트: 변경된 모든 줄은 사용자의 요청과 직접적으로 연결되어야 한다.

### 4. 목표 지향적 실행 (Goal-Driven Execution)

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 바꾼다:
- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과하도록 만든다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과하도록 만든다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과하는지 확인한다"

여러 단계로 이루어진 작업이라면 간단한 계획을 먼저 제시한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

성공 기준이 명확하면 독립적으로 반복 작업이 가능하다. "일단 되게만 해줘" 같은 모호한 기준은 계속되는 확인 질문을 필요로 한다.

(출처: [Andrej Karpathy의 코딩 관련 관찰](https://x.com/karpathy/status/2015883857489522876) 기반 가이드라인)
